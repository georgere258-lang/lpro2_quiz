name: lpro2
description: "LPro Real Estate Platform - Learn, Develop, PRO"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # Firebase & Authentication
  firebase_core: ^4.3.0
  firebase_auth: any
  cloud_firestore: ^6.1.1
  
  # UI Packages
  carousel_slider_plus: ^7.1.1
  intl_phone_field: any
  flutter_svg: any
  google_fonts: any

  # Storage & Preferences
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

# إعدادات أيقونة التطبيق
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"
  adaptive_icon_background: "#1B3358"
  adaptive_icon_foreground: "assets/logo.png"

flutter:
  uses-material-design: true
  
  # تم حذف قسم fonts المسبب للخطأ لاعتمادنا على google_fonts برمجياً
  assets:
    - assets/